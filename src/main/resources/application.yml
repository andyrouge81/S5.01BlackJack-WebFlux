server:
  port : 8080

spring:
  application:
    name: blackjack-reactive

  data:
    mongodb:
      uri: mongodb://localhost:27017/blackjack

  r2dbc:
    url: r2dbc:mysql//localhost:3306/blackjack
    username: root
    password: root

logging:
  level:
    org.springframework.data.mongdb.core: DEBUG
    org.springframework.r2dbc: DEBUG